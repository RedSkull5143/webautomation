package com.omshinde.pages.components;

import com.omshinde.pages.BasePage;
import org.openqa.selenium.WebDriver;

public class BaseCompnent extends BasePage {
    public BaseCompnent(WebDriver driver) {
        super(driver);
    }
}
